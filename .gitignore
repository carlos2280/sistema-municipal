# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
.next/
out/

# Turbo
.turbo/

# Environment - Variables locales (NO commitear)
# Cada servicio tiene su propio .env
.env
.env.local
.env.*.local
.env.development
.env.infra

# Excepciones - Templates y configs compartidas (SI commitear)
!.env.example
!.env.development.example
!**/.env.example

# IDE
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
logs/
*.log
npm-debug.log*
pnpm-debug.log*

# Testing
coverage/
.nyc_output/

# Docker
docker-compose.override.yml

# Temp
tmp/
temp/
*.tmp

# MCP Servers (contienen tokens locales)
mcp-servers/

# MCP config con tokens (usar .mcp.json.example como plantilla)
.mcp.json

# Claude — compartir settings, skills, hooks, agents, rules con el equipo
# Solo excluir settings locales (tokens personales, permisos extra)
.claude/settings.local.json

# NPM registry tokens
.npmrc.local

# Memory de Claude (vive en ~/.claude/projects/, no en el repo)
memory/

# Documentacion personal (disenos, prototipos, notas)
documentacion/

# Specs (personal por ahora)
specs/
