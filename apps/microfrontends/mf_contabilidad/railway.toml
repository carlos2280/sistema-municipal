# ============================================
# Railway Configuration - MF Contabilidad (Remote)
# ============================================

[build]
builder = "nixpacks"
watchPatterns = ["apps/microfrontends/mf_contabilidad/**", "packages/**"]

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
