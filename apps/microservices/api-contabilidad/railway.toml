# ============================================
# Railway Configuration - API Contabilidad
# ============================================

[build]
builder = "nixpacks"
watchPatterns = ["apps/microservices/api-contabilidad/**", "packages/**"]

[deploy]
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
